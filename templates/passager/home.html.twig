{% extends 'base.html.twig' %}

{% block title %}Page d'accueil Passager{% endblock %}

{% block body %}
    <style>
        body {
            font-family: 'Heebo', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f7fc;
        }

        /* Hero Section */
        .hero-section {
            background: linear-gradient(to bottom right, #007bff, #00c6ff);
            color: white;
            text-align: center;
            padding: 100px 20px;
            position: relative;
            overflow: hidden;
        }

        .hero-section h1 {
            font-size: 3rem;
            font-weight: 700;
            text-transform: uppercase;
        }

        .hero-section p {
            font-size: 1.25rem;
            margin-top: 10px;
            font-weight: 300;
        }

        .hero-section .btn {
            font-size: 1.1rem;
            padding: 10px 20px;
            text-transform: uppercase;
            font-weight: bold;
            margin-top: 20px;
        }

        .btn-primary {
            background-color: #4b84ff;
            border: none;
        }

        .btn-outline-light {
            border: 2px solid white;
            color: white;
        }

        .btn-primary:hover, .btn-outline-light:hover {
            background-color: #ffffff;
            color: #007bff;
        }

        .cta-buttons {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .cta-buttons a {
            text-decoration: none;
            background-color: #007bff;
            padding: 15px 30px;
            margin: 0 10px;
            font-weight: bold;
            color: white;
            border-radius: 50px;
            text-transform: uppercase;
            transition: background-color 0.3s ease;
        }

        .cta-buttons a:hover {
            background-color: #0056b3;
        }

        .cta-buttons a:active {
            background-color: #00408c;
        }

        .footer {
            background-color: #f8f9fa;
            padding: 20px;
            text-align: center;
            color: #343a40;
        }

        .footer a {
            color: #007bff;
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .hero-section h1 {
                font-size: 2.5rem;
            }

            .hero-section p {
                font-size: 1.1rem;
            }
        }
    </style>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <h1>Bienvenue Passager</h1>
            <p>Réservez vos trajets, payez en toute sécurité, et partagez vos retours pour une expérience de covoiturage améliorée.</p>
        </div>
    </section>

    <!-- Call to Action Buttons -->
    <div class="cta-buttons">
        <a href="{{ path('app_trajet_index') }}" class="btn btn-primary">Voir Les trajets Disponible</a>
        <a href="{{ path('app_reservation_index') }}" class="btn btn-primary">Voir vos réservations</a>
        <a href="{{ path('app_paiement_new') }}" class="btn btn-primary">Voir vos paiements</a>
        <a href="{{ path('app_feedback_new') }}" class="btn btn-outline-light">Ajouter un feedback</a>
    </div>

    <!-- Footer Section -->
    <div class="footer">
        <p>&copy; 2024 Application de Covoiturage. Tous droits réservés.</p>
        <div>
            <a href="#">Confidentialité</a> | <a href="#">Conditions d'utilisation</a> | <a href="#">Contact</a>
        </div>
    </div>

{% endblock %}
